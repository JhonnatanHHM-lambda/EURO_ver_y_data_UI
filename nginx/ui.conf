# Nginx config — vyd.lambdaanalytics.co (UI React)
# Aplica en el servidor: cp nginx/ui.conf /etc/nginx/sites-available/euro-vyd-ui
# Luego: ln -s /etc/nginx/sites-available/euro-vyd-ui /etc/nginx/sites-enabled/
# Luego: certbot --nginx -d vyd.lambdaanalytics.co && systemctl reload nginx

server {
    server_name vyd.lambdaanalytics.co;

    client_max_body_size 50M;

    access_log /var/log/nginx/euro-vyd-ui.access.log;
    error_log  /var/log/nginx/euro-vyd-ui.error.log;

    location / {
        proxy_pass http://127.0.0.1:3100;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host  $host;
        proxy_read_timeout  60s;
        proxy_connect_timeout 10s;
    }

    listen 80;
    listen [::]:80;
}
